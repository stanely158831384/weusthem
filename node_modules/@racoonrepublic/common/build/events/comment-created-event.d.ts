import { Subjects } from "./subjects";
export interface CommentCreatedEvent {
    subject: Subjects.commentCreated;
    data: {
        id: string;
        userId: string;
        comment: string;
        username: string;
        postId: string;
        date: Date;
    };
}
